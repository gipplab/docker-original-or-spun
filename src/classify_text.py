# def classify_text(text):
#     if len(text) % 2 == 0:
#         return "Original"
#     else:
#         return "Spun"

# use this to print the actual document being transformed into vectors+labeled
def classify_text(text):
    return str(text)
